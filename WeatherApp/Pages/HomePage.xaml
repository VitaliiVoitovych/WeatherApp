<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:WeatherApp.ViewModels"
             x:Class="WeatherApp.Pages.HomePage"
             x:DataType="viewModels:HomeViewModel">
    <VerticalStackLayout
        HorizontalOptions="Center">
        <HorizontalStackLayout
            HorizontalOptions="Center">
            <Label
                Text="{Binding Weather.Location.Name}"
                TextColor="{StaticResource Light}"
                FontSize="32"
                FontAttributes="Bold"/>
        </HorizontalStackLayout>
        <Image
            Source="{Binding Weather.Current, Converter={StaticResource WeatherIconConverter}}"
            HeightRequest="200"
            WidthRequest="200"/>

        <Label
            Text="{Binding Weather.Current.Condition.Text}"
            HorizontalTextAlignment="Center"
            FontSize="24"
            TextColor="{StaticResource Light}"/>

        <Label
            Text="{Binding Weather.Current.Temp, StringFormat='Temperature: {0:F0}°C'}"
            FontSize="24"
            TextColor="{StaticResource Light}"/>
        <Button 
            Text="TestButton"
            Background="{StaticResource SecondaryBrush}"
            Command="{Binding TestCommand}"/>
    </VerticalStackLayout>
</ContentPage>